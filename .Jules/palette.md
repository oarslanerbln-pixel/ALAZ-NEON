## 2026-08-12 - ARIA labels for icon-only buttons
**Learning:** Icon-only buttons (like × or +) are read literally by screen readers (e.g. 'times' or 'plus'), which provides no context for the action.
**Action:** Always add explicit aria-label attributes to icon-only buttons to describe their function (e.g., 'Kategoriyi Sil').

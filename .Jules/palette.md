## 2026-08-30 - Add aria-label to PhoneAuth Country Selector\n**Learning:** Added an explicit `aria-label` to the country code selection button in `PhoneAuth.tsx` to ensure that screen readers can correctly interpret the icon/abbreviation-based buttons (e.g., 'TR', 'DE'). This is a recurring pattern where interactive elements without descriptive text need to be made accessible.\n**Action:** Ensure all similar icon-based or abbreviation-based buttons include descriptive `aria-label` attributes.

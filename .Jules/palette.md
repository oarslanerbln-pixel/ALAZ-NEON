## 2026-09-06 - Adding Accessible Aria Labels
**Learning:** Navigation buttons (such as back buttons) should always have an `aria-label` associated with them for better screen reader accessibility, even if they have text labels, to ensure a smooth and accessible UX.
**Action:** Ensure all interactive elements, especially navigation buttons, are checked and updated with the proper `aria-label` attributes.
